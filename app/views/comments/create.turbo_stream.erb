<%= turbo_stream.append :comments, partial: 'comments/comment', locals: { comment: @comment } %>

<%#= turbo_stream.update :new_comment, partial: 'comments/form', locals: { post: @post, comment: @post.comments.new } %>